---
/**
 * BaseLayout - The main layout wrapper for all pages
 * GENERATIVA - AI Agents Platform
 *
 * Includes:
 * - HTML document structure
 * - Meta tags and SEO
 * - Schema.org structured data
 * - Global styles and scripts
 * - Analytics
 */

import { getTranslations, getHtmlLang, buildAlternatePath, type Locale } from '@i18n/index';
import Header from '@components/common/Header.astro';
import Footer from '@components/common/Footer.astro';
import CookieBanner from '@components/common/CookieBanner.astro';
import ScrollToTop from '@components/common/ScrollToTop.astro';
import '@styles/global.css';

interface Props {
  title: string;
  description: string;
  locale: Locale;
  canonicalUrl?: string;
  ogImage?: string;
  noIndex?: boolean;
  schema?: object;
}

const {
  title,
  description,
  locale,
  canonicalUrl = Astro.url.href,
  ogImage = '/images/og-image.png',
  noIndex = false,
  schema,
} = Astro.props;

// Force noindex on staging/preview deployments
const isStaging = import.meta.env.PUBLIC_STAGING === 'true' ||
                  Astro.url.hostname.includes('vercel.app');
const shouldNoIndex = noIndex || isStaging;

const t = getTranslations(locale);
const htmlLang = getHtmlLang(locale);
const alternateLocale = locale === 'ro' ? 'en' : 'ro';
const currentPath = Astro.url.pathname;
// Use buildAlternatePath for correct path translation (e.g., /ro/despre/ → /en/about/)
const alternatePath = buildAlternatePath(currentPath, locale, alternateLocale);

// Full URLs - Updated for GENERATIVA
// TODO: Update to generativa.ro when domain is purchased
const siteUrl = 'https://diginest-ai-platform.vercel.app';
const fullCanonicalUrl = canonicalUrl.startsWith('http') ? canonicalUrl : `${siteUrl}${canonicalUrl}`;
const fullAlternateUrl = `${siteUrl}${alternatePath}`;
const fullOgImage = ogImage.startsWith('http') ? ogImage : `${siteUrl}${ogImage}`;

// Default Organization schema - GENERATIVA
const organizationSchema = {
  '@context': 'https://schema.org',
  '@type': 'Organization',
  '@id': `${siteUrl}/#organization`,
  name: 'GENERATIVA',
  url: siteUrl,
  logo: {
    '@type': 'ImageObject',
    url: `${siteUrl}/images/generativa-logo.png`,
    width: 512,
    height: 512,
  },
  description: t.site.description,
  address: {
    '@type': 'PostalAddress',
    addressCountry: 'RO',
    addressRegion: 'Cluj',
    addressLocality: 'Dezmir, Comuna Apahida',
    streetAddress: 'Str. Viorelelor, nr. 17A',
  },
  legalName: 'DIGITAL LEADERSHIP SRL',
  taxID: '38585123',
  contactPoint: {
    '@type': 'ContactPoint',
    contactType: 'sales',
    email: 'contact@generativa.ro',
    availableLanguage: ['Romanian', 'English'],
  },
};

// Merge with page-specific schema
const pageSchema = schema ? [organizationSchema, schema] : [organizationSchema];
---

<!doctype html>
<html lang={htmlLang} class="scroll-smooth">
  <head>
    <!-- Basic Meta -->
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />

    <!-- Primary Meta Tags -->
    <title>{title}</title>
    <meta name="title" content={title} />
    <meta name="description" content={description} />

    <!-- Robots - noindex on staging/preview deployments -->
    {shouldNoIndex ? (
      <meta name="robots" content="noindex, nofollow" />
    ) : (
      <meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1" />
    )}

    <!-- Canonical & Hreflang — correct path translation between locales -->
    <link rel="canonical" href={fullCanonicalUrl} />
    <link rel="alternate" hreflang="ro" href={locale === 'ro' ? fullCanonicalUrl : fullAlternateUrl} />
    <link rel="alternate" hreflang="en" href={locale === 'en' ? fullCanonicalUrl : fullAlternateUrl} />
    <!-- x-default → RO (primary audience is Romanian market) -->
    <link rel="alternate" hreflang="x-default" href={locale === 'ro' ? fullCanonicalUrl : fullAlternateUrl} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={fullCanonicalUrl} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={fullOgImage} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:locale" content={locale === 'ro' ? 'ro_RO' : 'en_US'} />
    <meta property="og:locale:alternate" content={locale === 'ro' ? 'en_US' : 'ro_RO'} />
    <meta property="og:site_name" content="GENERATIVA" />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:url" content={fullCanonicalUrl} />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={fullOgImage} />

    <!-- Favicon - GENERATIVA logo -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/png" sizes="200x200" href="/images/generativa-logo-200.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/images/generativa-logo-200.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <meta name="theme-color" content="#22d3ee" />

    <!-- Preconnect to important third-party origins -->
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

    <!-- Google Fonts - Space Grotesk for display text -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;600;700&display=swap" rel="stylesheet" />

    <!-- Schema.org JSON-LD -->
    {pageSchema.map((schema) => (
      <script type="application/ld+json" set:html={JSON.stringify(schema)} />
    ))}

    <!-- Analytics (Plausible - privacy-friendly) -->
    <!-- TODO: Uncomment and update domain when Plausible is configured -->
    <!-- <script defer data-domain="generativa.ro" src="https://plausible.io/js/script.js"></script> -->
  </head>

  <body class="min-h-screen flex flex-col bg-white text-neutral-900 antialiased relative">
    <!-- Subtle ambient color blobs (visible through semi-transparent header) -->
    <div class="pointer-events-none fixed inset-0 z-0 overflow-hidden" aria-hidden="true">
      <div class="absolute -top-20 -left-20 w-[500px] h-[500px] bg-primary-200/25 rounded-full blur-3xl"></div>
      <div class="absolute top-10 right-0 w-[450px] h-[450px] bg-accent-200/20 rounded-full blur-3xl"></div>
      <div class="absolute top-[500px] left-1/4 w-96 h-96 bg-primary-100/25 rounded-full blur-3xl"></div>
    </div>

    <!-- Skip to main content link for accessibility -->
    <a
      href="#main-content"
      class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:px-4 focus:py-2 focus:bg-primary-400 focus:text-white focus:rounded-lg"
    >
      {locale === 'ro' ? 'Sari la conținut' : 'Skip to content'}
    </a>

    <!-- Header -->
    <Header locale={locale} currentPath={currentPath} />

    <!-- Main Content -->
    <main id="main-content" class="flex-1 relative z-10">
      <slot />
    </main>

    <!-- Footer -->
    <Footer locale={locale} />

    <!-- Cookie Banner -->
    <CookieBanner locale={locale} />

    <!-- Scroll to Top Button -->
    <ScrollToTop locale={locale} />

    <!-- Scripts -->
    <script>
      // Mobile menu toggle
      document.addEventListener('DOMContentLoaded', () => {
        const menuButton = document.getElementById('mobile-menu-button');
        const mobileMenu = document.getElementById('mobile-menu');

        if (menuButton && mobileMenu) {
          menuButton.addEventListener('click', () => {
            const isExpanded = menuButton.getAttribute('aria-expanded') === 'true';
            menuButton.setAttribute('aria-expanded', (!isExpanded).toString());
            mobileMenu.classList.toggle('hidden');
          });
        }
      });
    </script>
  </body>
</html>
