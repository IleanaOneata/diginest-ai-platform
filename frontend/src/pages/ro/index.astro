---
/**
 * Romanian Homepage - GENERATIVA
 *
 * Strategie "Demo-First + Storytelling"
 * Flow NOU (vezi docs/STRATEGY.md secțiunea 7.4):
 *
 * 1. HeroInteractive - Demo chat în primele 5 secunde
 * 2. BenefitsStrip - Impact vizual instant (6 beneficii)
 * 3. Services - Ce oferim
 * 4. UseCases - "Ți se pare familiar?" (auto-identificare)
 * 5. IntegrationHub - "Ce se întâmplă în spate" (vizual) [DARK]
 * 6. Process - Cum funcționează [LIGHT — separator între dark sections]
 * 7. ScaleWithConfidence - Animație wave + metrici business [DARK]
 * 8. FAQ - Întrebări frecvente
 * 9. CTA - Contact
 */

import BaseLayout from '@layouts/BaseLayout.astro';
import HeroInteractive from '@components/sections/HeroInteractive.astro';
import BenefitsStrip from '@components/sections/BenefitsStrip.astro';
import Services from '@components/sections/Services.astro';
import UseCases from '@components/sections/UseCases.astro';
import IntegrationHub from '@components/sections/IntegrationHub.astro';
import ScaleWithConfidence from '@components/sections/ScaleWithConfidence.astro';
import Process from '@components/sections/Process.astro';
import FAQ from '@components/sections/FAQ.astro';
import CTA from '@components/sections/CTA.astro';
import { getTranslations } from '@i18n/index';

const locale = 'ro';
const t = getTranslations(locale);

// Page metadata
const title = `${t.site.name} - ${t.site.tagline}`;
const description = t.site.description;

// Site URL (matches BaseLayout)
const siteUrl = 'https://diginest-ai-platform.vercel.app';

// Combined schema using @graph pattern
const pageSchema = {
  '@context': 'https://schema.org',
  '@graph': [
    // WebSite schema (helps Google Search)
    {
      '@type': 'WebSite',
      '@id': `${siteUrl}/#website`,
      url: siteUrl,
      name: 'GENERATIVA',
      description: t.site.description,
      inLanguage: ['ro-RO', 'en-US'],
      publisher: {
        '@id': `${siteUrl}/#organization`,
      },
    },
    // WebPage schema
    {
      '@type': 'WebPage',
      '@id': `${siteUrl}/ro/#webpage`,
      url: `${siteUrl}/ro/`,
      name: title,
      description: description,
      inLanguage: 'ro-RO',
      isPartOf: {
        '@id': `${siteUrl}/#website`,
      },
      about: {
        '@id': `${siteUrl}/#organization`,
      },
    },
    // BreadcrumbList (homepage = single item)
    {
      '@type': 'BreadcrumbList',
      '@id': `${siteUrl}/ro/#breadcrumb`,
      itemListElement: [
        {
          '@type': 'ListItem',
          position: 1,
          name: 'Acasa',
          item: `${siteUrl}/ro/`,
        },
      ],
    },
  ],
};
---

<BaseLayout
  title={title}
  description={description}
  locale={locale}
  canonicalUrl="/ro/"
  schema={pageSchema}
>
  <HeroInteractive locale={locale} />
  <BenefitsStrip locale={locale} />
  <Services locale={locale} />
  <UseCases locale={locale} />
  <IntegrationHub locale={locale} />
  <Process locale={locale} />
  <ScaleWithConfidence locale={locale} />
  <FAQ locale={locale} />
  <CTA locale={locale} />
</BaseLayout>
