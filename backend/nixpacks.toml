# =============================================================================
# Nixpacks Configuration for Railway Deployment
# =============================================================================
# Minimal config - let Nixpacks auto-detect Java/Maven from pom.xml.
# Only override the start command to pass Spring profile.

[start]
cmd = "java -jar -Dserver.port=$PORT target/*.jar"
